import React, { createContext } from "react";

export const handleImageContext = createContext();

const ImageContext = ({ chid }) => {
  return <div>ImageContext</div>;
};

export default ImageContext;
